# ---------------------------------------------------------------------------
# Data & outputs.
#   data/raw/       licensed SAS OpRisk / Advisen base datasets   -> NOT published
#   data/processed/ text-mined ransomware sample derived from them -> NOT published
#   output/         calibration / IBNR / SCR artifacts            -> committed, so
#                   the published results reproduce without the proprietary data.
# ---------------------------------------------------------------------------
data/raw/*
!data/raw/.gitkeep
data/processed/
# obsolete layout (kept on disk, never published)
data/generated/

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/
.venv/

# ---------------------------------------------------------------------------
# Editor / OS / tooling
# ---------------------------------------------------------------------------
.vscode/
.claude/
.DS_Store

# Backup archives (proprietary, do not publish)
*.zip
__MACOSX/